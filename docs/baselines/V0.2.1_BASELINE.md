# 元枢 V0.2.1 基线定义

状态：候选，等待最终干净源码独立验收与 Git 标签。

## 权威标识

- 产品版本：V0.2.1。
- 保护分支：`codex/v0.2-baseline-freeze`。
- 正式标签：`v0.2.1-baseline`（仅在全部验收通过后创建）。
- 冻结前父提交：`23b0760cac636ffbdd81dfa71a9b97ba593b33c1`。
- 标签指向的提交是版本源码的权威标识，不在同一提交内写死自身哈希。

## 安装包

- V0.2.0 旧安装包 SHA-256：`57C3BC2B345E44DE519352CC795A1868231E255CA4F59C1D65FA269598CF4889`。
- V0.2.1 候选安装包 SHA-256：`EFF2D43E7C7A06605D0A56D8C46C76C0403A8DD15C17F394DBD4ABB5E7903560`。
- 两个安装包均未做数字签名。
- 安装包和发布目录不提交 Git；保护副本位于阶段 0 外部快照，最终哈希以本文件为准。

## 已验证事实

- 旧 V0.2.0 安装目录的 533 个产品发布文件与冻结前发布目录同路径、同 SHA-256；安装目录额外 3 个文件是卸载器和显式用户数据删除脚本。
- 新 V0.2.1 发布目录与旧目录都有 533 个文件，路径集合完全相同；515 个运行时/第三方文件哈希不变，18 个元枢自有程序集或 deps 清单因版本与修补变化。
- V0.2.0 与 V0.2.1 都通过隔离安装、Host `--run-once`、数据库初始化、卸载、重装和用户数据保留验收。
- 当前机器上的 V0.2.0 正式安装和真实用户数据库未被修改。

## 重建命令

```powershell
dotnet restore ScreenGuide.slnx --locked-mode --disable-parallel
dotnet build ScreenGuide.slnx --configuration Release --no-restore --maxcpucount:1
dotnet test ScreenGuide.slnx --configuration Release --no-build --no-restore --maxcpucount:1
pwsh -NoProfile -File .\scripts\build-desktop-release.ps1
pwsh -NoProfile -File .\scripts\test-desktop-installer.ps1
```

真实窗口激活和 Windows Graphics Capture 测试必须在可交互的 Windows 桌面会话运行；受限沙箱结果不能代替真实桌面验收。

## 最终冻结条件

- 从候选提交的干净导出副本完成 locked restore、Release build、298 项测试、发布和安装验收；
- 把独立验收结果写入《元枢 V0.2 基线冻结报告》；
- 创建 `v0.2.1-baseline` 标签并确认工作区无未提交变化。
