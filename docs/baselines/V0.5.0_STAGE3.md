# 元枢 V0.5.0（V2 阶段 3）基线定义

状态：**源码冻结候选已通过；正式标签与标签源码产物身份待两提交冻结流程回填。** 当前机不运行同 AppId 安装—卸载—重装，以保护已安装版本、卸载登记和用户数据；因此本基线不等于安装包已获对外分发放行。

## 1. 权威身份

- 产品版本：V0.5.0。
- 阶段：V2 阶段 3“可控长期记忆”。
- 开发/集成分支：`codex/v2-stage2-ai-router`。
- 正式标签：`v0.5.0-stage3`（待创建并回填）。
- 标签目标源码提交：待回填。
- annotated tag object：待回填。
- 上一正式标签：`v0.4.0-stage2`，目标 `33b5859dcaa697bacd5edc5036a58d162b723a0e`。
- Desktop IPC：v10。
- SQLite schema：v10。

冻结使用两提交：先提交完整源码与冻结候选文档并创建标签，再从标签的独立干净源码执行 locked restore 和 Release build，最后用标签之后的仅文档提交回填标签对象、产物身份和哈希。标签后的文档提交不改变标签源码或二进制来源。

## 2. 正式能力边界

- 用户显式新增、查看、修正、启停和确认删除本机长期记忆；标题和正文使用 Windows DPAPI CurrentUser 专用边界保护后进入 SQLite。
- 本地相关记忆预览只由用户点击触发，使用确定性有界词法匹配，不保存查询或搜索索引，不调用模型。
- 普通聊天默认选择 0 条记忆。只有用户为单个 Turn 选择记忆、查看完整 Provider/Model/HTTPS origin/项目/标题/正文并单次确认后，才可向该 Turn 已冻结 Provider 最多发送一次。
- `chat.general@2` 把临时记忆 JSON 明确标记为不可信参考数据；`intent.semantic`、电脑动作、项目/文件/窗口授权和 Codex 编程任务永不接收记忆。
- 不自动提取、后台检索、自动注入、retry、fallback 或 resend；不包含 RAG、Embedding、向量数据库、画像、云同步或跨 Session 自动个性化。

## 3. 验收证据

- S3-R1 / R2 / R3：`INTEGRATED_PASS`，集成源 SHA `7318df2b81a17fcca9c39f2516d55f8d3ae8ce78`。
- exact-SHA QA 与 Security Gate：PASS；协议、schema、Prompt、权限、回滚和敏感数据边界通过。
- Stage 3 离线 Release 定向门禁：90/90 PASS（Tasking 29、DesktopHost 42、DesktopClient 15、AI Core Prompt 4）。
- 实际 Release WPF DesktopClient + DesktopHost：2/2 PASS；既有 Stage 2 路由/凭据/Session 回归保持通过，Stage 3 覆盖 CRUD、启停、本地预览、完整出站确认、单次 Fake Provider 调用、输入变化失效和删除。
- DesktopProduct 验收只使用隔离数据目录、fake secret 和进程内 Fake Provider：RealProviderRequests=0、CredentialReads=0、NetworkRequests=0。
- DesktopHost / DesktopClient Release build：待最终候选重跑并回填。

## 4. 干净标签源码产物

- 标签源码 locked restore：待回填。
- Release build：待回填。
- 发布目录文件数：待回填。
- DesktopClient ProductVersion / FileVersion：待回填。
- DesktopHost ProductVersion / FileVersion：待回填。
- 安装包：`artifacts/release/元枢-V0.5.0-安装包.exe`。
- 文件大小：待回填。
- SHA-256：待回填。
- 数字签名：待回填。

`artifacts/` 不进 Git。正式产物必须以本文档中的标签目标、SHA-256 和测试记录建立对应关系。

## 5. 回滚与未放行项

- V0.4.0 只支持 schema v8，不能直接打开 V0.5.0 schema v10。直接 v8→v10 使用 `pre-v10-from-v8`；v9→v10 使用 `pre-v10-from-v9`。回滚必须保留 v10 主库并在隔离目录使用匹配来源的备份。
- 本机不运行同 AppId 安装—卸载—重装；对外分发前仍需在干净 Windows 环境完成该生命周期。
- 安装包未签名时 Windows 会显示未知发布者；商业分发前还需数字签名和语音模型许可证/更新方案。

## 6. 冻结判定

- [x] S3-R1 / R2 / R3 功能集成、QA 与 Security Gate 通过。
- [x] Stage 3 离线 Release 定向测试与实际 Release DesktopProduct smoke 通过。
- [x] 真实网络、真实 Provider 请求与真实凭据读取均为 0。
- [ ] `v0.5.0-stage3` annotated tag、独立干净标签源码构建和产物身份待回填。
- [ ] 同 AppId 干净机安装生命周期和对外分发放行未完成。
