# 元枢 V0.3.0（V2 阶段 1）基线定义

状态：**阶段 1 已通过。** 363/363 自动化与最终 Release 真实桌面验收均已通过；最终源码提交、`v0.3.0-stage1` 标签和提交后重建安装包的 SHA-256 待版本冻结时补入。

## 1. 权威标识

- 产品版本：V0.3.0。
- 阶段名称：V2 阶段 1“统一会话中枢”。
- 开发分支：`codex/v2-stage1-session-coordinator`。
- 正式标签：`v0.3.0-stage1`（待最终冻结创建）。
- 最终源码提交：**待最终冻结填入**。
- 前一正式基线：`v0.2.1-baseline`。
- 前一基线标签提交：`8265ed8360a21658266545f63d6a4855c8dd4670`。
- Desktop IPC 协议：v7。
- SQLite schema：v7。

标签指向的提交是 V0.3.0 源码的最终权威标识。不要为了在同一提交中写入自身哈希而循环修改；最终冻结记录可填写标签解析得到的提交。

冻结采用两提交记录：先提交完整源码与文档，并在该提交创建 `v0.3.0-stage1`；再从标签目标重建安装包、计算 SHA-256，最后用单独的文档证据提交回填标签提交与安装包哈希。第二个提交只补证据，不改变标签所指源码。

## 2. 安装包与发布产物

- 预期安装包：`artifacts/release/元枢-V0.3.0-安装包.exe`。
- 安装包 SHA-256：**待最终冻结填入**。
- 数字签名：未签名，Windows 可能提示未知发布者。
- 本机安装生命周期：为保护已安装的 V0.2.0（Client/Host `0.2.0.0`，提交 `23b0760`）及其卸载登记和用户数据，本阶段没有在该机器运行同 AppId 的 `test-desktop-installer.ps1`。这是一项安全保护，不是产品功能或自动化测试失败；发布目录与实际 Release 用户流程已经验收，完整安装—卸载—重装仍应在干净机执行。

安装包和 `artifacts/` 不进入 Git。正式产物必须存入受保护的外部快照，并用本文件中的 SHA-256、源码标签和测试记录建立对应关系。

## 3. 本版本正式能力范围

- 唯一 SessionCoordinator 统一普通聊天、受控电脑动作、单窗口观察和编程任务的 Session / Turn 状态。
- 首页输入默认继续当前 Session 和同一 Conversation / Provider Thread；显式“新话题”创建新 Session。
- 应用/Host 重启恢复当前 Session；运行中的不安全工作中断且不自动重放。
- 用户停止或新输入真正取消 Provider / Codex 进程树、窗口观察或真实编程 Task。
- Conversation 取消与成功提交争夺 SQLite 唯一终态；每 Turn 的确认、授权和取消串行处理。已取消 Turn 拒绝迟到 Assistant 消息与迟到成功，不能覆盖新回答。
- 缺项目、文件、窗口或窗口同意时，保存原始请求并在同一 Turn 补齐后继续。
- 项目只能来自现有授权，已撤权的 Session 项目会清除并重新等待；文件必须存在且仍需本次确认；窗口同意绑定本 Turn 显示的窗口句柄、进程名和标题。
- 编程任务与普通聊天共享 Session，但使用独立 Turn；后台编程任务不阻止用户继续聊天。
- 电脑操作页的已发现应用/网站入口同样创建 Session Turn；应用绑定已发现的 Application ID，网站绑定规范化完整 HTTPS URI。Host 在初次规划和确认执行两处精确比较 Expected Intent/Target 与 Canonical Plan Target，不匹配则结束 Turn 且不执行。
- UI 统一显示理解、回答、执行、等待上下文、取消、完成、失败和中断状态。
- protocol v7 的 `sessions.wait` 长轮询提供增量 Session 更新；Coordinator 实例 ID、启动时间与版本区分 Host 重启前后的新旧快照。Named Pipe 服务有连接上限、忙碌响应、首请求超时、监听重试和坏请求帧隔离。
- SQLite schema v7 持久化 Session / Turn 及 Expected/Plan Target，并在从 v5 或中间 v6 迁移前自动备份数据库。
- V0.2.1 的确定性意图、权限白名单、已授权项目、文件确认、单窗口同意和敏感动作禁令不变。

## 4. 明确不包含

- DeepSeek 接入或 DeepSeek 模型路由。
- 更换普通聊天模型或第二个可用的通用聊天 Provider。
- 用户长期记忆、RAG、向量数据库、跨 Session 个性化检索。
- 复杂多 Agent 产品功能、手机端、云同步或远程控制。
- 开放式模型 Tool Calling 或扩大 V0.2.1 动作白名单。
- Prompt 集中注册、版本化和完整评测体系；这些属于后续阶段。

## 5. 自动化与构建

| 验证项 | 冻结结果 |
|---|---|
| locked restore | 通过 |
| Release build | 通过 |
| 自动化测试总数 | **363/363 通过** |
| 失败 / 跳过 | 0 / 0 |
| Release publish | 通过 |
| NuGet 已知漏洞检查 | 通过；官方源检查全 solution，无已知易受攻击的直接或传递依赖 |
| 本机安装—卸载—重装 | 安全保护下未执行；避免同 AppId 覆盖现有 V0.2.0 卸载登记与数据 |

自动化项目分布：Agent.Codex 19、AI.Core 20、Core 92、DesktopClient 36、DesktopHost 112、Tasking 50、Voice 14、Vision 7、Skills 11、DesktopProduct 2，共 363。

阶段 1 新增或强化的测试边界包括：

- SessionStore 迁移、当前 Session、项目上下文和重启恢复；
- 连续十轮、Provider Thread 复用和明确新话题；
- Provider 取消、进程树终止、数据库终态竞争、迟到结果拒绝和快速双输入；
- 项目、文件、窗口与确认在同一 Turn 续接；
- 编程任务与聊天并发、真实 Task 取消和任务状态对账；
- 幂等、跨 Session 越权、确认/取消竞态、并发双确认、失效项目回退和目标变化后重新确认；
- 提交与新话题/切换并发、确认与替代输入并发、窗口同意与第二条输入并发；
- `GitHub Desktop` 等应用名的应用/网站目标替换、同 Intent 相近名称与恶意 URI 子串回归；
- Session 长轮询、无 Session 等待、IPC 压力、静默连接释放与坏请求帧隔离；
- Session UI 状态映射、旧版本/旧 Host 快照拒绝和实际 Release WPF UI Automation。

## 6. 已确认真实验收

### 连续十轮

- 环境：实际 Release DesktopClient + DesktopHost + 真实 Codex。
- 结果：10/10 通过。
- 覆盖：“刚才那个”“第二个”“继续”“不是这个，我说的是……”以及对前面一句话的引用。
- 结论：十条用户输入保持同一 Session，并复用同一 Conversation / Provider Thread；未出现每句话重新开始。

### 真取消

- 环境：实际 Release DesktopClient + DesktopHost + 真实 Codex。
- 结果：3/3 通过。
- 每次取消后继续观察 10 秒；没有旧回答重新出现，没有旧回答覆盖新状态。
- 自动化还验证 UI 停止会终止 FakeCodex 子进程树，而不是只隐藏界面。

### 上下文补齐与窗口授权

- 项目补充：同一 Session、同一原始 Turn 选择已授权项目后自动继续，并执行真实隔离编程任务，通过。
- 文件补充：同一 Session、同一原始 Turn 选择存在文件后继续并经过可见确认，通过。
- 真实 Notepad 窗口同意：只查看用户同意的单个窗口，Turn 为 `Completed`，通过。
- 真实 Notepad 窗口拒绝：不捕获、不继续执行，Turn 为 `Cancelled`，通过。
- 最终 Release 综合场景 5/5，`Failed=0`、`FalseCompleted=0`。

### 独立桌面产品验收

- 结果：2/2 通过。
- 覆盖实际 Release WPF DesktopClient/Host、Session UI 与 Host 状态同步、停止按钮真取消、单窗口授权拒绝、Client/Host 生命周期与恢复。

### 证据位置

- 真实 Codex 与上下文流程：`%LOCALAPPDATA%\ScreenGuide\Experiments\DesktopV01\20260823-182601`。
- 目录包含隔离项目、隔离用户数据、SQLite 和 Host 日志；它是本机运行证据，不属于 Git 源码。

## 7. 数据兼容与迁移

- V0.3.0 首次打开 schema v5 数据库时，会先在同目录创建 `tasking.pre-v7-from-v5-<时间>.backup.db`，再迁移到 schema v7；若从开发中间态 schema v6 升级，则生成 `tasking.pre-v7-from-v6-<时间>.backup.db`。
- V0.3.0 保留原有 Conversation、Task、授权、审计和 Evidence 数据，并新增 Session / Turn 表。
- schema v7 为 Session Turn 新增 Expected Intent/Target 与 Plan Target，并有 v6 → v7 迁移和备份测试。
- V0.2.1 会拒绝 schema v7；因此“程序可以回滚”不代表“旧程序可以直接打开新数据库”。

## 8. 回滚到 V0.2.1

回滚必须保留当前 V0.3.0 源码、产物和用户数据库，不能使用 `git reset --hard`、`git clean` 或直接覆盖现有数据库。

1. 源码回滚：在新的干净目录或 Git worktree 中检出 `v0.2.1-baseline`；不要改变当前阶段 1 工作区。
2. 程序回滚：使用 V0.2.1 基线文档记录的安装包和 SHA-256，先在隔离测试环境确认。
3. 数据回滚：停止 Client/Host，完整备份当前 V0.3.0 用户数据；把自动生成的 pre-v7 数据库备份复制到独立数据目录后验证。不要让 V0.2.1 打开 schema v7 正式数据库。
4. 验收回滚：验证 Host 启动、数据库版本、原项目授权、历史任务和低风险动作，再决定是否切换正式安装。

V0.2.1 的精确源码、测试与安装包记录见 [V0.2.1_BASELINE.md](V0.2.1_BASELINE.md)。

## 9. 干净源码重建

```powershell
dotnet restore ScreenGuide.slnx --locked-mode --disable-parallel
dotnet build ScreenGuide.slnx --configuration Release --no-restore --maxcpucount:1
dotnet test ScreenGuide.slnx --configuration Release --no-build --no-restore --maxcpucount:1
pwsh -NoProfile -File .\scripts\build-desktop-release.ps1
pwsh -NoProfile -File .\scripts\test-desktop-installer.ps1
```

真实窗口、UI Automation 和 Windows Graphics Capture 测试必须在可交互的 Windows 桌面会话运行；受限沙箱不能代替真实桌面验收。

## 10. 版本冻结记录

- [x] 阶段 1 功能、363/363 自动化和真实 Release 桌面验收通过。
- [x] 官方 NuGet 源全 solution 已知漏洞检查通过。
- [x] V0.2.1 回滚使用 `v0.2.1-baseline` 与 pre-v7 备份/隔离数据目录的边界已记录。
- [x] 已记录本机不运行同 AppId 安装生命周期测试的安全原因；干净机完整安装—卸载—重装留作发布分发验收。
- [ ] 最终源码提交已填入。
- [ ] `v0.3.0-stage1` 标签已创建。
- [ ] 提交后重建安装包的 SHA-256 已填入。

后三项完成后，本文件即可同时作为 V0.3.0 的精确源码与安装包身份记录。
