# V0.6.0 / V2 Stage 4 冻结基线

## 状态

- 阶段：Stage 4 Final Freeze
- 版本：V0.6.0
- 状态：**FINAL FREEZE / INTEGRATED_PASS**
- 正式标签：`v0.6.0-stage4`
- C0 源码提交 / TagTarget：`3a591a7b6af7da7d97e07093d4c33a3f44553b82`
- annotated tag object：`20045c7960c182a052a5e0b2552ce0ed14a3863f`
- 独立标签源码离线 locked restore 与 `build-desktop-release.ps1 -SkipTests` 发布/安装包编译：PASS
- C1：仅回填冻结证据，不改变标签源码或二进制身份

V0.6.0 / Stage 4 的源码与产物身份已经冻结。该状态不代表同 AppId 生命周期、数字签名、语音模型许可或对外分发已经放行。

## C0 包含的事实

- S4-R1 Window Identity v2：Host 可信窗口身份包含 HWND、PID、进程启动时间、进程名和标题；schema v11，身份变化逐层失败关闭。
- S4-R2 本机真实使用评测：固定合同下真实语音 18/20、独立 STOP 取消通过；真实视觉 20/20、identity-change 失败关闭。该证据不等于所有设备、口音或窗口内容的普遍质量保证。
- S4-R3 有界 Session 投影：protocol v11，bootstrap/delta/reset、消息 keyset 分页、精确 Turn 查询、Client 有界缓存和持久化提交后的 Task 状态唤醒均已集成；SQLite 仍是状态真源。
- S4-R4 runtime gate 有界化：Session/Turn 临时门闩通过 holder+waiter 引用计数和同实例归零移除回收；SessionCoordinator 仍是 Session/Turn 唯一所有者。
- S4-R1/R2/R3/R4 均已 `INTEGRATED_PASS`；Final Freeze 不启动 S4-R5 或 Stage 5。

## 固定版本与安装包身份

- `Version`：`0.6.0`
- `AssemblyVersion`：`0.6.0.0`
- `FileVersion`：`0.6.0.0`
- `InformationalVersion`：`0.6.0`
- 安装包文件名：`元枢-V0.6.0-安装包.exe`
- AppId：保持既有值，不在 C0 改变

独立干净标签源码产物身份：

- PublishFileCount：539
- DesktopClient ProductVersion：`0.6.0+3a591a7b6af7da7d97e07093d4c33a3f44553b82`
- DesktopClient FileVersion：`0.6.0.0`
- DesktopHost ProductVersion：`0.6.0+3a591a7b6af7da7d97e07093d4c33a3f44553b82`
- DesktopHost FileVersion：`0.6.0.0`
- InstallerBytes：64,203,075
- InstallerSha256：`5F912F94960E1E90A1EF918139C46751FCA8377A4055B5E68BA060A9BF4E56D7`
- SignatureStatus：`NotSigned`

## 协议、数据库与回滚

- Desktop IPC：protocol v11。
- SQLite：schema v11。
- V0.5.0 只支持 schema v10，不能直接打开 schema v11。
- 回滚到 V0.5.0 时，DesktopHost 与 DesktopClient 必须作为匹配的一对同时回滚；不得混用 v10/v11 Host 与 Client。
- 回滚必须保留 v11 主库，不得覆盖、替换或原地降级。
- 只允许在隔离数据目录使用与 schema 来源匹配的 `pre-v11-from-v10`；没有匹配备份时必须失败关闭。
- 若要继续回滚到 Stage 2/Stage 1，仍必须分别遵守 pre-v10/pre-v8 等历史边界，不能用当前源码冒充历史冻结版本。

## 发布边界

- 本机同 AppId 安装—卸载—重装生命周期尚未在隔离干净环境放行。
- 安装包尚未获得数字签名。
- 语音模型许可、下载与分发方案尚未放行。
- 因此 Final Freeze 不能声称已经获准对外分发，也不得把冻结证据误写成签名或安装生命周期验收。
- 冻结流程没有新增真实 Provider 请求、凭据读取、GUI/麦克风验收或历史真实证据重跑。

## 两提交冻结流程（已完成）

1. 已创建并核验完整源码/版本/预冻结文档提交 C0。
2. Coordinator 已创建并核验 annotated tag `v0.6.0-stage4`。
3. 已复用 S4-R1～R4 与 C0 QA 冻结的 exact-SHA targeted 测试证据；独立干净标签源码的离线 locked restore、`build-desktop-release.ps1 -SkipTests` Client/Host publish 与安装包编译通过。
4. 本 C1 仅回填真实 C0/tag object/产物身份和最终门禁，不改变标签源码或二进制来源。

## 最终冻结门禁

- [x] C0 exact SHA 已由 Git 事实确认
- [x] annotated tag `v0.6.0-stage4` 已创建并核对 tag object/target
- [x] 已复用冻结的 exact-SHA targeted 测试证据，独立干净标签源码离线 locked restore 与 `build-desktop-release.ps1 -SkipTests` 通过
- [x] Client/Host ProductVersion 与标签提交绑定
- [x] 发布文件数、安装包大小、SHA-256 和签名状态已从标签源码产物记录
- [x] C1 仅文档证据提交记录上述事实，不改变标签源码或二进制身份
- [ ] 同 AppId 干净机生命周期、数字签名和语音模型许可/对外分发尚未放行

Stage 4 状态为 **FINAL FREEZE / INTEGRATED_PASS**；未勾选项是对外分发边界，不否定源码与产物身份冻结。
