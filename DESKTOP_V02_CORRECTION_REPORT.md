# 元枢 Desktop V0.2 纠偏与实现报告

> 历史资料：本文记录 2026-08-19 的 V0.2.0 实现与当时验收，不再作为当前事实来源。当前事实请查阅 `PRODUCT.md`、`ARCHITECTURE.md` 和 `docs/baselines/V0.2.1_BASELINE.md`。

日期：2026-08-19

## 1. 纠偏结论

此前界面把“连续文字对话”、文本提交框和 Codex 放在产品中心，不符合“统一桌面 AI 中枢”的定位。本轮已改为常驻语音入口：用户打开元枢后无需按键或输入文字，AI Core 自动接收完整语音、判断意图并执行开放的低风险动作；普通问答不操作电脑，敏感能力仍单独确认，编程任务才路由给 Codex。

Codex 当前保留两种角色：

1. 已授权 Git 项目的编程技能；
2. 在尚未配置独立通用模型供应商时，作为只读文字问答 Provider 的临时实现。

Windows 应用、网页、文件和可靠搜索操作不经过 Codex，也不依赖 Codex 是否安装。

## 2. 最终架构

```text
DesktopClient
  ├─ 可见的常驻语音入口
  ├─ sherpa-onnx 端点检测 / 自动分句
  └─ 敏感能力的可见计划与一次性确认
            ↓ 当前用户 Named Pipe / protocol v6
DesktopHost
  ├─ AI Core / IntentPlanner
  ├─ AssistantCommandService / 单次计划容器
  ├─ CapabilityPolicyEngine / 一次性授权
  ├─ Windows 安全技能
  │    ├─ 已安装应用目录
  │    ├─ https 网页
  │    ├─ 明确选择的文件
  │    ├─ 唯一可靠搜索框
  │    └─ 单窗口可访问控件说明
  ├─ WindowUnderstandingService / 显式同意与内存生命周期
  │    ├─ 单个 HWND 捕获（绝不回退到整个桌面）
  │    ├─ Windows 本机 OCR
  │    ├─ UI Automation 结构辅助
  │    └─ 敏感窗口拒绝 / 停止即取消 / 图像用后清零
  ├─ Conversation Provider 接口
  └─ 编程技能 → CodexConnector → TaskEvidence
```

DesktopClient 不直接访问 SQLite、Codex 或 Windows 操作实现；所有动作通过 Host 的统一服务边界和审计入口。

## 3. 已实现能力

- 统一语音首页替代“文字聊天中心 + 电脑操作页”的中心导航；
- 首页不再显示文本框、按住说话、发送文字、选择文件或项目下拉框；
- 窗口首次显示后自动监听，端点检测到一句完整语音后自动处理；关闭到托盘后继续待命，真正退出后立即释放麦克风；
- 本机 sherpa-onnx 中文流式识别，录音不保存；
- 明确的低风险语音指令在说完后直接规划并执行，不再要求按键或“理解并准备 → 确认这一次”；
- 可选 Windows 中文语音朗读结果；
- 确定性意图识别：普通问答、打开应用、打开网站、打开文件、前台搜索、前台窗口说明、编程任务；
- 语音只直接授权打开应用、打开网站和可靠前台搜索；动作计划只消费一次，Host 重启后不重放；
- 从 Windows AppsFolder、开始菜单、桌面快捷方式和 Windows App Paths 发现应用，不扫描硬盘；这使千问等没有传统快捷方式登记的已安装程序也能被识别；
- 文件只能由用户本次选择；
- 网站只允许不含账号信息的 `https` 地址；
- 搜索只操作唯一识别、可写、非密码的 UI Automation 搜索控件；
- 窗口说明在本次显式同意和确认后读取绑定的单个窗口，结合本机 OCR 与 UI Automation 说明应用、界面类型和主要文字；
- 捕获图像只存在于短生命周期内存，不写入数据库、日志或文件，不发送云端，分析完成、失败或取消都会清零；
- 密码输入框、Windows 凭据、安全验证和支付类窗口会被拒绝；界面显示当前目标窗口并提供“停止查看”；
- 窗口识别结果明确列出局限，不把纯图片、图标、视频或复杂布局误报成已经可靠理解；
- 结构化 `AssistantActionEvidence`，明确区分“已核实执行”和“只收到 Windows 启动请求”；
- 首次启动不再要求 Codex，也不再要求按键试音或输入示例任务，改为能力检查、自动监听说明和安全边界；
- 托盘“新建任务”改为统一新指令入口；
- Codex 从主界面降级到“编程技能”设置和编程任务页。

## 4. 安全边界

- 模型输出、窗口文字、网页和文档不能产生操作授权；
- 明确语音指令可作为本次低风险动作的一次性授权，授权不可复用；读取画面、打开文件和编程任务仍需可见确认；
- 不移动鼠标，不执行任意命令，不点击任意按钮；
- 不发送、不发布、不支付、不删除、不改账号安全设置、不输入密码或凭据；
- 常驻监听状态始终在首页可见，音频只在内存中识别且不保存；不实现唤醒词或未显示界面的静默开机捕获；
- 不捕获整个桌面，不自动扫描硬盘；
- 审计不保存网页 query/fragment 或前台搜索词正文；
- 关闭到托盘后语音中枢继续待命；从托盘真正退出后停止监听，同时 UI 关闭不影响 Host 已管理的编程任务恢复证据。

## 5. 测试结果

Release 全量测试：298/298 通过，失败 0。Windows 界面类测试按顺序运行，避免多个测试争用同一桌面会话。

### 语音直接执行纠正

- 首页显示后自动开始监听，识别端点自动形成一条完整指令并处理，不存在按住、松开或文字提交步骤；
- “打开应用”“打开网站”“在当前窗口搜索”三类明确低风险语音指令直接执行，语音内容作为本次一次性授权写入审计来源；
- 普通问题直接进入回答流程；读取窗口画面、打开具体文件和编程任务仍显示确认，不允许语音绕过；
- 独立只聊天入口继续从正式导航隐藏，避免用户进入无电脑权限的错误通道；
- 打开应用现在必须发现、恢复并显示真实窗口，不能只凭进程启动就声称完成。

### 浏览器搜索、应用记忆与语音插话纠正

- “在文字栏搜索抖音”“在浏览器文字栏里搜索抖音”“在地址栏搜索……”和“在当前窗口的搜索栏查找……”现在都会进入可靠前台搜索，不再误判为普通聊天；
- 已用真实 Windows 可访问文本框验证：元枢能够唯一识别“地址和搜索栏”，写入关键词并提交 Enter；如果控件不唯一、不可写或是密码框，仍会安全拒绝；
- 应用目录增加 Windows AppsFolder 注册信息，但不扫描硬盘、不硬编码个人安装路径；本机已登记的千问可以按名称进入应用清单；
- 新语音到达时立即清除上一条计划和结果，取消正在处理的旧指令，只保留最新一句，避免“说千问却仍显示抖音结果”；
- 朗读期间继续监听；“停”“等一下”“不对”等插话会优先停止朗读和上一条处理，正常长度的新指令也会直接抢占；同时过滤元枢自身朗读回声；
- 朗读音量由最大值降到适中值，为本机麦克风保留声学余量，降低必须大声说话才能被识别的概率。

### 浏览器可见操作纠正

- 修复复合语音指令丢失指定浏览器的问题；“打开谷歌浏览器界面并搜索打开抖音”现在会规划为一次明确的“Google Chrome + 抖音网站”操作；
- 指定 Chrome 或 Edge 时使用新的浏览器窗口打开目标网站，不再退化成系统默认浏览器；
- 启动后等待对应浏览器顶层窗口，恢复最小化窗口并将其带到用户可见的最前层；
- 只有窗口可见性经过验证才记录 `ExecutionVerified`；找不到或无法显示窗口时操作失败，不再把“Windows 接受启动请求”误报为完成；
- 新增完整 Host 链路测试、指定浏览器安全门禁测试、真实 Windows 合成窗口激活测试，以及“启动独立模拟浏览器进程 → 发现窗口 → 恢复并置前 → 精确清理测试进程”的端到端验收；窗口激活测试连续 5 次通过。

| 测试层 | 数量 | 结果 |
|---|---:|---|
| Agent Codex | 18 | 通过 |
| AI Core | 17 | 通过 |
| Core / 权限 / 状态机 | 92 | 通过 |
| DesktopClient | 22 | 通过 |
| DesktopHost / IPC / Evidence | 69 | 通过 |
| Desktop 产品进程验收 | 1 | 通过 |
| Windows 安全技能 | 11 | 通过 |
| Tasking / SQLite | 47 | 通过 |
| Voice Windows | 14 | 通过 |
| Window Vision Windows | 7 | 通过 |

窗口读取真实低风险验收：创建一个只含固定测试文字、没有个人数据的临时窗口，实际完成单 HWND 捕获、本机 OCR/标题分析、PNG 内存签名校验与用后清理；未向磁盘写入截图。

安装验收：安装、Host 初始化、卸载、重装、最终卸载退出码均为 0；数据库初始化成功；卸载与重装保留历史数据。

## 6. 发布与本机安装

- 安装包：`artifacts/release/元枢-V0.2.0-安装包.exe`
- SHA256：`57C3BC2B345E44DE519352CC795A1868231E255CA4F59C1D65FA269598CF4889`
- 安装目录：`%LOCALAPPDATA%\Programs\YuanshuDesktop`
- 用户数据：沿用 `%LOCALAPPDATA%\ScreenGuide\V01`，升级未删除历史
- 语音模型：沿用 `%LOCALAPPDATA%\ScreenGuideTeacher\models`

本机新版 DesktopClient 与 DesktopHost 已启动并响应；已安装的 Client、Host、AI Core、Windows Skills 和 Voice Windows 文件哈希均与本次发布产物一致。语音程序集、sherpa-onnx 与 onnxruntime 均已安装。

正式安装版真实低风险验收：通过 Desktop Host 的统一入口执行“打开千问”，千问窗口真实显示；随后执行“打开谷歌浏览器”和“在文字栏搜索抖音”，Chrome 的真实窗口标题变为“抖音 - Google 搜索”，可访问的地址搜索栏也确认包含该关键词。三次动作均返回 `ExecutionVerified`，没有绕过 Host 直接操作。

## 7. 尚未完成或明确暂不做

- 没有实现任意桌面控制；这是安全边界，不是遗漏；
- 当前实现的是“单窗口本机文字与结构理解”，能够回答正在使用的应用、窗口标题、常见界面类型和主要可读文字；尚不能可靠理解纯图片、图标含义、视频内容和复杂空间关系；
- 当前首选 Windows Graphics Capture 绑定单一 HWND，并已用真实临时窗口验证；不支持或取帧失败时只回退到同样绑定单一 HWND 的 `PrintWindow`，绝不回退为整个桌面截图；
- 当前没有云端多模态模型，因而不会把窗口图像上传给任何供应商；若未来加入必须单独展示数据去向并再次征得同意；
- 没有实现唤醒词和电话式全双工语音；当前已经实现可见常驻监听、朗读期间本机插话和最新指令优先，但不同电脑的扬声器回声与麦克风灵敏度仍需真实使用校准；
- 没有接入独立的通用商业模型 Provider；普通问答当前仍使用 `IConversationProvider` 后的 Codex 只读临时实现；
- 没有把语音模型放入安装包；当前本机可复用已有模型，商业分发前必须核验模型许可证和下载/更新方案；
- 没有手机端、云端、推送、远程桌面、视频和长期记忆。

## 8. 下一阶段建议

在继续扩大动作范围前，先由用户在新版界面完成 20—50 次可见、低风险真实使用，记录 OCR 漏读、界面类型误判、敏感窗口拒绝率和确认是否易懂。之后再评估 Windows Graphics Capture 后端与可选多模态 Provider；不要先扩展任意点击与输入。
