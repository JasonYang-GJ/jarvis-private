# 元枢 V2 分阶段路线图

> 路线图是计划，不代表已经实现。每个阶段必须独立验收，失败时不得自动进入下一阶段。

## 阶段 0：冻结真实基线

状态：已通过（2026-08-23）。

目标：保护 V0.2 成果，建立源码、测试、安装包、Git 标签和文档之间的可恢复对应关系。

通过条件：

- 外部快照和 Git bundle 可恢复；
- 85 个原始变化全部分类；
- 锁定依赖可恢复，Release 构建成功；
- 真实桌面全量测试 298/298；
- V0.2.0 旧安装/发布关系与 V0.2.1 新安装包关系得到验证；
- 从最终 Git 标签的干净源码副本可重建、测试和生成安装包；
- 唯一事实来源完整，保护分支工作区干净。

## 阶段 1：统一会话中枢

状态：未开始，等待项目负责人确认。

目标：让语音、文字、电脑动作、窗口上下文和编程任务共享统一的 Session / Turn（会话 / 一轮交互）状态机，先解决“执行不连续”和“上下文分裂”。

拟交付：

- 统一 Session、Turn、Intent、Plan、Execution、Result 数据模型；
- 明确“当前上下文、任务状态、历史决策、用户长期信息”的边界；
- 兼容并迁移现有 SQLite 对话和任务数据；
- 保留现有一次性权限和安全白名单；
- 加入中断、恢复、幂等和多步骤真实用户验收。

非目标：本阶段不顺带扩大桌面操作权限，不同时建设完整长期记忆或多 Provider 路由。

## 阶段 2：Prompt 与模型 Provider 标准化

状态：未开始。

- Prompt 集中注册、版本化、测试用例和回归评测；
- 统一模型 Provider 接口、能力描述、错误恢复、超时和成本/Token 记录；
- 至少用第二个真实 Provider 验证可替换性。

## 阶段 3：可控长期记忆

状态：未开始。

- 用户长期事实、项目状态、任务状态和历史决策分库存储；
- 相关性检索、来源、置信度、过期、冲突、纠错和删除；
- 不把全部聊天历史无脑塞回模型。

## 阶段 4：体验与分发加固

状态：未开始。

- 真实使用评测、语音/视觉失败率和延迟数据；
- 语音模型许可证与下载更新方案；
- 安装包数字签名、升级/回滚和发布自动化；
- 在证据充分后再评估新的低风险动作范围。
