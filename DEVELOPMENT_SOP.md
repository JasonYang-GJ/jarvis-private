# 元枢开发与验收流程

本文只记录元枢项目已经采用的协作、Git、安全和验收流程。产品能力、现状、架构与路线图仍分别以 `PRODUCT.md`、`ARCHITECTURE.md`、`MEMORY.md` 和 `ROADMAP.md` 为准，本文不能替代这些来源。

## 标准交付链

1. 项目总经理整理需求、范围、禁止项和验收标准。
2. 架构师确认实现边界、公共测试缝隙和是否允许修改数据合同。
3. 总经理取得项目负责人批准，并向 Developer 发出带基线 SHA 和文件白名单的正式工作单。
4. Developer 只在指定独立 Git worktree 和专属分支工作；开始前核对物理目录、分支、HEAD 和干净状态。
5. Developer 按测试驱动开发执行 RED→GREEN 小循环，保存真实失败与通过证据，不用实现细节断言替代行为证明。
6. Developer 完成范围扫描、敏感数据扫描、相关回归、Release 构建和可运行的真实桌面验收后，创建独立提交并交付准确 Commit SHA。
7. QA 必须对该 SHA 独立验收。没有通过 QA Commit Gate 的提交不得集成。
8. QA FAIL 时，Developer 在原分支追加修复提交，不 amend、rebase、squash 或改写已审查历史；QA 对新 SHA 完整重验。
9. QA PASS 后，总经理只以 fast-forward-only 方式集成批准的提交，并在集成分支运行集成回归。
10. 集成回归与要求的真实验收通过后，总经理再更新完成报告；候选实现、自动化通过或 Developer 自测都不能单独宣称阶段完成。

## Git 与 worktree 规则

- 总经理先冻结基线和变更证据，再建立干净检查点；Developer 从批准的 SHA 开始。
- Developer 不在主协调目录修改，不切换或覆盖总经理分支，不覆盖其他 Agent 或用户的未提交内容。
- 每个工作单只修改批准白名单；提交前审查 staged diff、运行 `git diff --check`，提交后工作区必须干净。
- Developer 不自行合并，不向远程仓库 push，不创建未经批准的发布标签。
- QA 失败只追加新提交；保留失败候选与修复提交之间的可追溯关系。
- 回滚不使用破坏性 reset 或 clean。需要回退时使用干净导出或独立 worktree，并保留当前数据库和可达基线。

## QA Commit Gate

QA 验收对象必须是 Developer 报告中的精确 Commit SHA。至少核对：

- SHA、父 SHA、分支和工作区状态；
- 实际变更文件是否等于批准范围；
- RED 证据是否在修复前成立，GREEN 是否通过同一公共边界；
- 定向测试、必要回归、Release 构建和工作单要求的完整套件；
- 安全、隐私、取消、幂等、失败关闭和历史兼容是否没有被测试替身掩盖；
- 真实桌面测试按工作单要求串行运行，环境限制与代码失败分开报告；
- 无密钥、令牌、密码、用户数据库、截图、录音、日志、ETL 或本机绝对路径进入提交。

## Scope Change 门禁

如果白名单内无法实现，Developer 必须停止修改并提交以下提案，等待架构师判断和总经理显式批准：

```text
SCOPE-CHANGE-<工作单> PROPOSED
- 原批准范围：
- 当前阻断和真实 RED/编译证据：
- 请求新增的精确文件或模块：
- 白名单内替代方案为何不能满足合同：
- 安全、数据、兼容和回归风险：
- 不批准时的失败关闭方案：
- 建议新增验收命令和通过标准：
```

未批准前不得先改后报，也不得借重构、测试便利或临时诊断扩大永久范围。

## 决策与沟通边界

- 产品方向、阶段范围、架构体系、数据库合同、权限边界、对外发布和预算属于重大决定，必须返回用户或项目负责人确认。
- 架构分歧由总经理转交架构师，不由 Developer 另起一套体系。
- 普通技术问题、工作单澄清和 Agent 协调由总经理处理；只有会改变用户目标、重大风险或授权范围的决定才必须询问用户。
- Developer 报告已验证事实、合理推断、未验证项和遗留风险，不把“能编译”写成“已经完成”。

## 数据库与回滚保护

- 不接触用户真实数据库；迁移测试使用临时隔离数据库和公开 Store 接口。
- schema 版本、迁移、连接策略、备份合同和回滚行为只有在工作单与架构批准明确覆盖时才能修改。
- 新版本数据库必须保留工作单要求的旧版备份；旧程序不能直接打开不兼容的新 schema。
- 安装器或卸载器验收不得覆盖需要保留的同 AppId 版本；使用干净机器或隔离 Windows 环境。
- 清理测试数据前确认精确路径和所有者，不以 GC、任意等待、吞异常或清空连接池掩盖资源生命周期缺陷。

## 敏感数据与真实验收

- Key、Token、Password、Cookie、凭据、用户正文、私人路径和环境变量不得写入日志、测试输出、SQLite、IPC 回包或 Git。
- 测试只使用假凭据、canary 文本和隔离目录；扫描发现疑似敏感内容必须在提交前解释并排除。
- 真实 Provider、真实桌面、真实取消和安装器验收按工作单分别执行；模拟 Provider、Fake CLI 或单元测试不能替代它们。
- 会争用前台、焦点、剪贴板、UI Automation 或系统捕获服务的真实桌面测试必须串行运行，并记录桌面会话和权限条件。
