# 元枢产品事实（V0.2.1 基线）

> 当前产品事实的唯一入口。更新时间：2026-08-23。

## 产品定位

元枢是 Windows 本机学习与操作助手。它在用户可见、明确授权的前提下理解当前窗口，给出中文帮助，并只执行少量经过白名单限制的低风险动作。

## 已实现并纳入 V0.2.1 基线

- Windows 桌面客户端与独立 Host 进程；两者只通过当前用户的 Named Pipe 通信。
- 可见的本机离线中文语音监听、自动分句、语音朗读、朗读时插话和回声过滤。
- 确定性意图识别：普通问答、打开已登记应用、打开受校验的 HTTPS 网站、在可靠搜索框搜索、读取当前单窗口、打开本次选择的文件、执行已授权 Git 项目中的编程任务。
- 明确语音只对“打开应用、打开网站、可靠搜索”产生一次性授权；窗口读取、打开文件和编程任务仍需要单独的可见确认。
- 已安装应用发现、指定 Chrome/Edge 打开网页、前台窗口恢复与可见性核验。
- 仅操作 Windows UI Automation 唯一识别的可写搜索框；拒绝密码框和无法唯一识别的控件。
- 仅捕获用户确认的单个窗口；本机 OCR 与 UI Automation 联合分析；图像只在短期内存中存在，使用后清零。
- SQLite 保存任务、项目授权、审计、对话消息、对话轮次和外部 Provider Thread ID。
- 编程任务通过 Codex Connector 执行，并记录 Git、测试和 TaskEvidence 证据。
- 自包含 win-x64 发布目录、简体中文 Inno Setup 安装包、隔离安装/卸载/重装验收脚本。

## 当前 AI 大脑的真实状态

- 普通问答走 `IConversationProvider` 可替换接口，但 V0.2.1 实际只注册 `CodexConversationProvider`。
- 该 Provider 启动本机 Codex CLI 的只读对话进程；实际使用哪个底层模型由 Codex 自身配置决定，元枢没有固定模型 ID。
- 电脑动作不由大模型自由决定，而由本机确定性规划器和权限策略判断后执行。
- DeepSeek 没有接入运行时代码，也没有 API Key、Endpoint 或模型路由。`prompts/DEEPSEEK_REVIEW.md` 只是历史代码审查模板。

## 尚未完成

- 没有统一会话中枢；语音指令、普通对话、电脑动作和编程任务仍是多条并行流程。
- 没有真正的用户长期记忆、相关信息检索、记忆纠错、过期管理或跨会话个性化。
- Prompt 没有集中注册、版本号、变更记录和自动评测；普通对话 System Prompt 仍内嵌在 Codex Provider 代码中。
- 普通问答仍依赖本机 Codex CLI，尚未有第二个可实际切换的通用模型 Provider。
- 本机单窗口理解主要依赖 OCR 和可访问控件，不能可靠理解纯图片、视频、图标语义和复杂空间关系。
- 语音模型不在安装包内，商业分发前仍需完成许可证、下载和更新方案。
- 安装包未做数字签名；Windows 可能显示未知发布者警告。
- 没有手机端、云同步、远程控制或实时视频能力。

## 明确禁止或不开放

- 任意移动鼠标、任意点击或向任意输入框输入。
- 发送、发布、付款、删除、修改账号或安全设置、输入密码或凭据。
- 全桌面截图、隐藏捕获、全盘扫描、未授权远程控制。
- 把窗口画面上传到云端模型。

## 版本与安装状态

- 当前机器保留的已安装正式版：V0.2.0，未升级到 V0.2.1；阶段 0 只用原 V0.2.0 包做过同版本修复安装以恢复卸载登记，533 个产品文件和 5 个用户数据文件哈希均未变化。
- 阶段 0 新冻结的可继续开发基线：V0.2.1。
- 精确提交、测试、安装包和哈希以 `docs/baselines/V0.2.1_BASELINE.md` 及 Git 标签 `v0.2.1-baseline` 为准。
