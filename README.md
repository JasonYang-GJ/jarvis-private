<p align="center"><img src="assets/branding/yuanshu-icon.png" width="104" alt="元枢图标"></p>

# 元枢 Yuanshu

**Windows 本机 AI 学习与操作助手：用中文交流，理解你授权查看的窗口，完成少量明确、可控的电脑操作。**

A Windows desktop AI assistant for Chinese conversation, local window understanding, and explicitly authorized desktop actions.

元枢把文字/语音聊天、单窗口文字识别、应用与网站启动、受控搜索和授权项目中的编程任务放在一个桌面入口。适合探索“AI 怎样安全地协助日常电脑使用”的开发者和使用者。

**当前状态：开发中的公开源码项目。最近冻结版为 V0.6.0；V0.7.0 修复候选尚未完成真实指针问答闭环与正式分发验收。源码公开不等于正式安装版发布。**

[功能与限制](#能做什么) · [开发指南](docs/DEVELOPMENT.md) · [产品事实](PRODUCT.md) · [技术架构](ARCHITECTURE.md) · [路线图](ROADMAP.md) · [公开范围与权利说明](docs/PUBLIC_SOURCE_STATUS.md)

## 能做什么

| 功能 | 实际用途 | 状态与条件 |
| --- | --- | --- |
| 中文连续对话 | 继续上一个话题、补充问题、切换新话题 | 已实现；需自行配置 DeepSeek 或千问 |
| 本机语音识别与朗读 | 用中文提问、听取回答、朗读时插话 | 已实现；需麦克风及单独准备的语音模型 |
| 打开应用和网站 | 打开唯一识别的已安装应用、校验后的 HTTPS 网站 | 已实现；一次明确指令只授权一次动作 |
| 受控搜索 | 在可靠识别的搜索框或地址搜索栏填写并提交查询 | 已实现；无法唯一识别控件时拒绝执行 |
| 单窗口理解 | 读取当前授权窗口的文字和可访问控件，提供中文帮助 | 已实现；本机 OCR，不把窗口图片上传给模型 |
| 编程任务 | 在授权的 Git 项目中调用本机 Codex，记录修改与测试结果 | 已实现；需本机 Codex，保留项目和文件权限边界 |
| 可控本机记忆 | 手动管理事实、偏好、项目备注和决定 | 已实现；加密保存，选择并预览确认后才用于一次聊天 |
| 停止与恢复 | 取消后台工作，拒绝迟到结果；重启后保留会话状态 | 已实现；不自动重放未完成的危险操作 |
| 指针区域提问 | 指向文字 → 本机识别 → 完整预览 → 再确认发送纯文本 | 修复候选已实现，真实 OCR/模型闭环仍待验收 |

例如，你可以尝试“打开记事本”“继续解释刚才的问题”；查看窗口、打开文件或执行编程任务时，软件会要求对应的可见确认。具体可操作范围取决于应用是否提供可可靠识别的控件。

普通聊天与编程任务分别配置：切换聊天模型不会替你切换编程代理，也不会扩大文件或电脑操作权限。普通聊天的 Codex 适配器当前有意停用，编程功能仍使用独立 Codex 连接器。

## 数据与操作边界

- 只查看本次明确授权的单个窗口，不进行全桌面截图；窗口图像和音频不作为运行日志保存。
- 云端普通聊天会发送相应文字；记忆和指针识别文字有单独的出站预览与确认。API 调用费用由使用者自己的服务账号承担。
- 语音模型就绪时，主界面显示后会进入可见的本机监听状态。关闭到托盘可能继续待命；需要停止时使用停止功能或真正退出程序。
- API 密钥通过 Windows 当前用户加密保护，不放进源码、普通设置或数据库正文。
- 不提供任意鼠标移动、任意点击、密码输入、支付、删除、账号安全设置修改或无人监督的远程控制。
- 手机端、云同步、实时视频理解和复杂多代理产品能力仍不属于当前已实现范围。

## 开发运行

需要 Windows x64、Git，以及符合 [global.json](global.json) 的 .NET SDK（当前锁定 10.0.400，允许同功能带补丁更新）。窗口、语音和桌面界面验收需要可交互的 Windows 会话。

    git clone https://github.com/JasonYang-GJ/jarvis-private.git yuanshu
    cd yuanshu
    dotnet restore ScreenGuide.slnx --locked-mode --disable-parallel
    dotnet build ScreenGuide.slnx -c Release --no-restore --maxcpucount:1

构建后，按 [开发指南](docs/DEVELOPMENT.md) 指定独立的数据目录和配对后台，再启动桌面界面。在设置中配置自己的聊天服务；不要把密钥写入仓库。语音模型未捆绑，缺少模型时不能使用离线语音识别。编程任务需要另行准备本机 Codex 和项目授权。

当前没有面向普通用户正式发布的签名安装包，不应直接将历史内部候选安装到重要环境。

## 怎样工作

**文字 / 语音 / 可见确认 → Windows 界面 → 本机会话与任务中枢 → 对应能力**

后台分别连接普通聊天模型、本机窗口识别、低风险操作和授权项目中的编程任务，统一管理状态；每项能力仍执行自己的权限校验。

主要技术：**C# / .NET 10、WPF、Windows UI Automation、Windows Graphics Capture、SQLite、DPAPI、Sherpa-ONNX**。界面与后台进程分离，模型接口与权限执行分离。

## 验证状态

2026-09-12 的修复候选技术账本汇总 **1,262 / 1,262 项通过**，另有 **6 / 6 个合成窗口指针交互场景通过**。该数字由原全量通过项目与修复后受影响项目完整重跑组成，不是本次公开操作新跑出的 CI 结果。

这些结果不替代真实指针 OCR 与模型回答验收。详细依据和限制见 [Gate4C 修复记录](docs/V0.7.0_GATE4C_REPAIR.md)；历史冻结版本见 [基线目录](docs/baselines/)。私人测试记录和本机数据库不进入公开仓库。

## 项目导航

| 目录 / 文件 | 内容 |
| --- | --- |
| src/ | 产品源码：桌面界面、后台、模型、语音、视觉、存储与任务 |
| tests/ | 自动化测试、假模型与桌面测试工具 |
| prompts/runtime/ | 受版本与哈希校验的运行提示词 |
| tools/、scripts/ | 集成验收、回滚验证与构建工具 |
| distribution/、installer/ | 第三方材料、历史分发合同与安装定义 |
| [PRODUCT.md](PRODUCT.md) | 已实现能力和已知缺口 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 当前技术结构 |
| [ROADMAP.md](ROADMAP.md) | 后续计划，不能当成已实现能力 |

下一步优先完成真实指针问答验收；若准备交付其他电脑，再推进签名与最终分发验证。更多模型、灵动岛联合演示及手机方向按实际需求另行排期。

## 源码公开与许可

仓库现用于公开展示与技术审阅，**尚未选择项目级开源许可证**，不应标称为 MIT / Apache 开源项目。GitHub 平台允许的查看与 fork 按平台条款处理；其他使用、修改和分发许可需另行确认。第三方组件保留各自的 LICENSE / NOTICE。

历史冻结材料中的“仅专有编译产品、不公开源码”记录反映当时的发布决策；2026-09-14 所有者已另行授权公开本仓源码。该授权没有重写旧版本合同，也没有放行签名安装包、模型捆绑或商业分发。见 [公开范围与权利说明](docs/PUBLIC_SOURCE_STATUS.md)。
