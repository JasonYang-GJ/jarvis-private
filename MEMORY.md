# 元枢项目记忆

> 本文件记录稳定项目事实和历史决策，不是产品运行时的“用户长期记忆数据库”。更新时间：2026-08-23。

## 当前状态

- 当前稳定产品系列：V0.2。
- 当前机器已安装：V0.2.0，阶段 0 未覆盖、未卸载、未删除用户数据库。
- 阶段 0 候选基线：V0.2.1；完成独立干净源码验收并打 `v0.2.1-baseline` 标签后才算正式冻结。
- 下一候选阶段：阶段 1“统一会话中枢”；阶段 0 未通过时不得启动。

## 已确认决策

1. 渐进演进，不推倒重来。V0.2 中通过测试的窗口、权限、语音、存储和 Codex 连接能力必须迁移保留。
2. DesktopClient 不直接访问 SQLite、Codex 或 Windows 动作，DesktopHost 是唯一编排和审计边界。
3. 电脑操作采用确定性意图 + 权限白名单；模型不能自行扩大权限。
4. 只捕获用户确认的单个前台窗口，绝不回退为全桌面。
5. 运行数据存放在用户本地应用数据目录，不进入仓库；卸载默认保留。
6. 模型供应商必须位于可替换接口后。当前 Codex 只是唯一实现，不代表长期绑定。
7. 聊天记录、外部 Thread ID、任务状态和长期记忆是不同概念；阶段 1 不能继续混用。
8. 安装产物不提交 Git，以版本标签、哈希、测试记录和外部快照关联。
9. 本轮因修复基线测试和加入依赖锁而使用补丁版本 V0.2.1，避免两个不同内容都叫 V0.2.0。

## 阶段 0 新增事实

- 冻结前工作区：39 修改、2 删除、44 未跟踪，共 85 个变化。
- 85 个变化已全部分类：71 正式能力、11 保留后重构、3 历史遗留、0 构建产物、0 无法判断。
- 首次全量测试 295/298；1 项因受限环境无法访问真实 Windows Graphics Capture，2 项暴露真实稳定性问题。
- 最小修补包括：Host 使用自有文件日志而不触发非管理员 EventLog 权限、启动失败审计不被取消令牌吞掉、窗口激活复用已有稳健前台激活逻辑。
- 修补后真实桌面全量测试 298/298。
- V0.2.0 已安装目录中的 533 个发布文件与受保护旧发布目录逐一哈希一致；另有 3 个安装附加文件。

## 尚未解决

- 没有统一会话中枢和真正长期记忆。
- Prompt 无集中管理与版本评测。
- 通用对话只有 Codex Provider，DeepSeek 未接入运行时。
- UI、SQLite Store 和部分编排服务文件过大。
- 安装包无数字签名；语音模型分发与许可证待定。

## 更新规则

- 只记录已经确认、未来仍有用的事实和“为什么这样决定”。
- 路线图设想写入 `ROADMAP.md`，不能写成已实现。
- 当前功能写入 `PRODUCT.md`，当前结构写入 `ARCHITECTURE.md`。
- 每个正式版本必须新增基线文档和 Git 标签；不得覆盖旧版本记录。
